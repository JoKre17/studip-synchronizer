StudIP Synchronizer version 0.0.1


Installation as a service to be run on system start:
	Windows:
		1. Run Install-StudIPSynchronizer.bat (you need admin permission)
		
		
Deinstallation:
	Windows:
		1. Run Uninstall-StudIPSynchronizer.bat (you need admin permission)
		
Run the service:
	Windows:
			Run StudIPSynchronizer.bat
		or	Run StudIP Synchronizer.exe